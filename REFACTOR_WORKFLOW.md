/goal 次のリファクタリング・レビュー・テストワークフローを自律的に実行し、完了条件をすべて満たすまで継続する。

【目的】
[対象範囲]について、外部仕様・公開API・ユーザーから観測可能な動作を原則変更せず、保守性、可読性、テスト容易性、型安全性、重複、責務分離、エラー処理、性能上の問題を改善する。

【基本原則】
- メインエージェントはオーケストレーターとして、調査結果の統合、優先順位付け、合否判定、サブエージェントへの指示を担当する。
- 調査、実装、レビュー、テストには明示的に別のサブエージェントを起動する。
- 実装者自身に最終レビューや最終合否判定をさせない。
- 調査は並列実行してよいが、コード修正は原則として改善項目ごとに直列実行する。
- 無関係なファイル、公開仕様、依存関係、設定、生成物を不用意に変更しない。
- テストを削除、無効化、skip、期待値緩和することで合格扱いにしない。
- 各工程の証拠として、実行コマンド、結果、変更ファイル、主要なdiffを記録する。

【工程0：事前確認】
1. AGENTS.md、README、設計資料、ビルド設定、テスト設定を確認する。
2. 現在のビルド、lint、型検査、既存テストを実行し、変更前ベースラインを記録する。
3. 変更前から存在する失敗は、新規に発生した失敗と区別して記録する。

【工程1：改善点の洗い出し】
以下の観点ごとに、読み取り専用の調査サブエージェントを必要数起動し、全員の完了を待つ。
- アーキテクチャと責務分離
- 重複、複雑度、命名、可読性
- 型安全性、例外処理、境界値
- テスト不足、テスト困難性、flaky test
- 性能、並行処理、リソース管理
- セキュリティ上の懸念

結果を統合し、各候補について次を記載した改善バックログを作成する。
- ID
- 問題と根拠
- 対象ファイル
- 期待効果
- リスク
- 推定変更範囲
- 実施計画
- テスト項目
- 優先度

重複候補を統合し、効果、リスク、変更量を基準に優先順位を決定する。
仕様変更が必要なもの、根拠が弱いもの、効果よりリスクが高いものは実装せず、保留理由を記録する。

【工程2：改善項目ごとの修正ループ】
優先順位順に、改善項目を1件ずつ処理する。

1. 修正前に、その改善項目について具体的な修正計画と受入条件、対象テストを定義する。
2. 実装用サブエージェントを起動し、当該項目だけを最小差分で修正させる。
3. 実装用サブエージェントには、関連する限定テスト、lint、型検査を実行させ、変更内容と結果を報告させる。
4. 実装完了後、独立した読み取り専用レビューサブエージェントを起動する。
5. レビューサブエージェントには、計画、受入条件、git diff、関連コード、テスト変更を確認させ、PASSまたはFAILを根拠付きで報告させる。
6. メインエージェントが報告と実際のdiffを確認し、レビュー合否を判定する。

レビューFAILの場合：
- 指摘を重要度、ファイル、箇所、再現方法、期待する修正に整理する。
- 実装用サブエージェントへ具体的にフィードバックして再修正させる。
- 修正後は、必ず新しいレビューサブエージェントで再レビューする。
- 同一項目で3回不合格になった場合は、原因、試行内容、残課題を記録し、無理に変更を続けず保留または停止判定を行う。

レビューPASSの場合：
- テスト専用サブエージェントを起動する。
- 単体テスト、統合テスト、回帰テスト、lint、型検査、ビルドなど、定義したテストを実行させる。
- テスト専用サブエージェントは本番コードを変更せず、実行結果と失敗原因の分類を報告する。

テストFAILの場合：
- 製品コードの不具合なら、実装用サブエージェントへ戻し、修正→独立レビュー→テストを繰り返す。
- テストコードの欠陥なら、根拠を示した上でテスト修正担当サブエージェントに限定修正させ、その変更も独立レビューする。
- 環境、外部サービス、既存不具合が原因なら、新規変更による失敗かを切り分け、証拠を記録する。
- 原因不明のままテストを無効化しない。

テストPASSの場合：
- 当該改善項目を完了として記録し、次の改善項目へ進む。

【工程3：最終検証】
全改善項目の処理後、独立した最終レビューサブエージェントと最終テストサブエージェントを起動する。
以下を確認する。
- 全変更の横断レビュー
- 公開仕様と既存動作の回帰
- 全テスト
- lint
- 型検査
- ビルド
- 変更ファイルが意図した範囲内であること
- 不要なデバッグコード、コメントアウト、生成物がないこと

【完了条件】
次をすべて満たした場合だけ完了とする。
- 採用した改善項目がすべて実装済み、または合理的な保留理由がある。
- 各実装が独立レビューでPASSしている。
- 最終レビューがPASSしている。
- 変更による新規テスト失敗がない。
- 指定されたテスト、lint、型検査、ビルドがすべて成功している。
- 公開仕様に意図しない変更がない。
- docs/refactoring-report.md が作成されている。

【最終報告書】
docs/refactoring-report.md に以下を記載する。
- 対象範囲と目的
- 変更前ベースライン
- 洗い出した改善候補一覧
- 採用、保留、却下の判断と理由
- 各改善の計画、変更内容、対象ファイル
- 各レビューの指摘、修正、最終判定
- 実行したテストコマンドと結果
- 変更前から存在した失敗
- 残存リスクと今後の推奨事項
- 最終的な完了条件のチェック結果
